{{- if and .Values.server.enabled (eq (include "gov-jwks-service.server.pdbEnabled" .) "true") -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "gov-jwks-service.server.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "gov-jwks-service.server.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.server.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "gov-jwks-service.server.selectorLabels" . | nindent 6 }}
{{- end }}
